# side-chick

A vscode extension that will help navigate and understand new a codebase with help of claude. 

## Development

```sh
pnpm dev         # start with bun --watch
pnpm test        # run tests (vitest)
pnpm lint        # lint (oxlint)
pnpm build       # type-check
```
