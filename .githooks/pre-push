#!/bin/sh
echo "Running pre-push checks..."
corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test:run && corepack pnpm build || {
  echo "Pre-push checks failed. Fix before pushing."
  exit 1
}
